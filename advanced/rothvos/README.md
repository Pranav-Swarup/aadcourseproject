## Code Compilation Instructions

---

To run the code, please install the `glpk` package.

Using Pacman:

```bash
sudo pacman -S glpk
```

And compile the code using the `-lglpk` and `-std=c++11` flags
